# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
build/
develop-eggs/
dist/
downloads/
eggs/
.eggs/
lib/
lib64/
parts/
sdist/
var/
wheels/
share/python-wheels/
*.egg-info/
.installed.cfg
*.egg
MANIFEST
*.manifest
*.spec
pip-log.txt
pip-delete-this-directory.txt
htmlcov/
.tox/
.nox/
.coverage
.coverage.*
.cache
nosetests.xml
coverage.xml
*.cover
*.log
.hypothesis/
.pytest_cache/
celerybeat-schedule
celerybeat.pid

# Environments
.env
.venv
env/
venv/
ENV/
env.bak/
venv.bak/

# Django stuff:
*.mo
*.pot
db.sqlite3
db.sqlite3-journal
media/ # Usually user-uploaded, depends on your strategy
# staticfiles/ # Usually generated by collectstatic, don't commit (Nginx serves from volume)
             # If you have project-level static files you *do* want to commit (like images),
             # then keep those in DentalPrototype/static/ and ensure STATIC_ROOT (staticfiles/) is ignored.
             # The DentalPrototype/static/css/output.css *is* generated, so should ideally be .gitignored
             # and built during deployment or by each dev. However, for simplicity in some workflows,
             # people commit it. Let's lean towards ignoring it for now.

# Node / JavaScript
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
coverage/
dist/ # If you had a separate Vite build output in root

# Tailwind specific generated files (output.css IS generated)
static/css/output.css # Add this!

# Operating System Files
.DS_Store
Thumbs.db
Desktop.ini

# IDEs and editors
.idea/
.vscode/
*.swp
*~